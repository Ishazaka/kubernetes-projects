apiVersion: v1
kind: Service
metadata:
  name: mini-webapp-service

spec:
  type: NodePort

  selector:
    app: mini-webapp

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
