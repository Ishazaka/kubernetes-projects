apiVersion: v1
kind: Pod
metadata:
  name: backend-pod
  labels:
    app: backend
spec:
  containers:
    - name: backend-container
      image: mini-k8s-backend:v1
      imagePullPolicy: Never
      ports:
        - containerPort: 3000
